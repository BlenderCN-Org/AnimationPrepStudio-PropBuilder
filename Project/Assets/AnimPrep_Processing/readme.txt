The processing folder contains the files which are by the generated prefabs. 
This folder contains uniquely named UUID folders corrosponding to the prefab that uses it.
It stores files such as textures and .fbx models.